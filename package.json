{
  "name": "dimsum-steamer-queue",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "convex:dev": "convex dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "convex deploy --cmd \"vite build\" && wrangler deploy",
    "deploy:frontend-only": "vite build && wrangler deploy"
  },
  "dependencies": {
    "convex": "1.44.0",
    "lucide-react": "^0.383.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0",
    "wrangler": "^3.80.0"
  }
}
